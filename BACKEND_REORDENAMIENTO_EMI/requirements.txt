# ─────────────────────────────────────────────────────────────────────────────
# Dependencias de PRODUCCIÓN
# Instalación:  pip install -r requirements.txt
# Para desarrollo (linter, etc.):  pip install -r requirements-dev.txt
# ─────────────────────────────────────────────────────────────────────────────

# Core
Django==6.0.4
djangorestframework==3.17.1
djangorestframework_simplejwt==5.5.1
PyJWT==2.12.1
django-filter==25.2
django-cors-headers==4.9.0

# Documentación de API (OpenAPI / Swagger / ReDoc)
drf-spectacular==0.29.0

# Base de datos
psycopg2-binary==2.9.11

# Tareas asíncronas y caché
celery==5.6.3
redis==7.4.0

# Almacenamiento de objetos (S3 / MinIO)
boto3==1.35.70

# Generación de PDF y Excel (reportes)
reportlab==4.4.10
pillow==12.2.0
openpyxl==3.1.5

# Utilidades
python-decouple==3.8
requests==2.32.3
